# PROJECT_MAP — Restaurant Menu (المنيو الإلكتروني)

> آخر تحديث: 2026-08-10 | مسار الجذر: `restaurant-menu/` (مجلد فرعي، مجزول عن ملفات المتجر القائمة) | **+ `desktop/`**: لوحة إدارة OGTX لسطح المكتب (Electron)
> الوضع: **المرحلة الرابعة (قسم «المينيو») مكتملة ومُحقّقة (MD10)** — الأصناف/المنتجات/المكونات/الإضافات/مجموعات الإضافات + ترتيب DnD + تعديل جزئي يحافظ على الصورة + معاينة + صلاحيات حقيقية في main process على نفس قاعدة Turso. التالي: **المرحلة الخامسة** (أي الأقسام المتبقية: stock/stats/expenses/suppliers).
> سجل قرارات: **DB Runtime = `node:sqlite` محلياً + Turso (libSQL) على السحابة** — محلياً: `node:sqlite` (DatabaseSync) صفر تبعيات؛ عند النشر (Vercel): `@libsql/client` يتصل بقاعدة Turso عبر `TURSO_URL`/`TURSO_TOKEN`. الانتقال تلقائي في `lib/db.ts` (دوال async موحّدة).

## [TECH_STACK]

| طبقة | اختيار | الإصدار (تم التحقق 2026-08-08) | ملاحظات |
|---|---|---|---|
| Runtime | Node.js | **24 LTS (Krypton)** — 24.19.0 | مثبّت عبر winget |
| Framework | Next.js (App Router) | **16.3.0** (LTS, 2026-08-03) | React 19 + TS مدمجان |
| Styling | Tailwind CSS | **4.3.3** (2026-07-16) | تصميم داكن (dark-first) عبر tokens |
| Animations | Motion (خليفة framer-motion) | **13.0.0** (2026-08-05) | الاستيراد من `motion/react` |
| DB | SQLite: **`node:sqlite` محلياً** + **Turso (libSQL) عن بُعد** عبر `@libsql/client` | مدمج + 0.17.4 | `getDb()` يختار تلقائياً: `TURSO_URL` موجود → Turso، وإلا ملف `data/menu.db`؛ schema idempotent يُطبّق عند الإقلاع على كليهما |
| Auth | HMAC-session مكتوبة يدوياً (`node:crypto`) | — | بدون تبعيات؛ كلمة مرور واحدة من `.env` |

قواعد: لا تُستخدم أي حزمة Deprecated. أي إصدار ناقص يُثبَّت بأحدث Stable وقت التنفيذ ويُوثَّق هنا.

## [SYSTEM_FLOW]

**رحلة الزبون:** `/` (Hero ترحيبي + أنيميشن) → زر «تفقد الآن» → `/menu` (شبكة الأصناف بصور: مشروبات / بيتزا / برجر …) → `/menu/[slug]` (منتجات الصنف؛ زر «المكونات والإضافات» يفتح **Bottom Sheet** معزولاً بصرياً: قسم «المكونات» المشمولة قابلة للإزالة وقسم «الإضافات» الاختيارية) → إضافة إلى السلة (Context + localStorage، سطر فريد لكل تخصيص عبر `key`؛ الإضافة فورية + **أنيميشن طيران صورة الطبق إلى زر السلة ديناميكياً** `getBoundingClientRect` + نبضة أيقونة 🛒 + إشعار «أُضيف … إلى السلة») → درج السلة → «تأكيد الطلب» يُرسل Server Action يُخزّن الطلب في `orders` (بلا واتساب).

**رحلة الادمن:** `/admin` (تسجيل دخول بكلمة مرور واحدة) → HttpOnly Cookie موقّع HMAC → `/admin/dashboard` (قائمة أصناف قابلة للترتيب Drag & Drop + إضافة صنف + إخفاء/إظهار/تعديل اسم/حذف لكل صنف) → `/admin/categories/[slug]` (لوحة إدارة الصنف: اسم + إخفاء/إظهار + حذف + صورة + لوحة الرؤية؛ وكل طبق بأقسام أكورديون: معلومات/مكونات/إضافات/خيارات أخرى) → `/admin/orders` (قائمة طلبات الزبائن مع الحذف) → Server Actions (محروسة بالمصادقة) → `node:sqlite`.

**تدفق البيانات:** صفحات الخادم (Server Components) تقرأ `node:sqlite` مباشرة عبر `lib/db.ts`؛ الطفرات عبر Server Actions فقط؛ السلة Client-side (ترسل بنودها عند تأكيد الطلب ويُعاد حساب السعر خادمياً للمصادقة). الصفحات المقرؤة للبيانات `force-dynamic` كي تنعكس تعديلات الادمن فوراً.

## [ARCHITECTURE]

```
restaurant-menu/
├── app/
│   ├── layout.tsx / globals.css        # RTL + سمة داكنة + خطوط عربية (Cairo) + تذييل إنستغرام
│   ├── not-found.tsx                   # 404 عربي أنيق
│   ├── page.tsx                        # Hero ترحيبي (Motion)
│   ├── menu/page.tsx                   # شبكة الأصناف (بطاقات مربعة، عمودان على الهاتف؛ يفلتر isHidden)
│   ├── menu/[slug]/page.tsx            # منتجات صنف (يفلتر المخفي isHidden) + 404 للصنف المخفي + تمرير المكونات
│   ├── orders/actions.ts               # placeOrderAction: تحقق/حساب سعر خادمي + تخزين الطلب
│   ├── admin/page.tsx                  # تسجيل دخول (بوابة: مصادق → /dashboard)
│   ├── admin/actions.ts                # Server Actions محروسة: login/logout/CRUD/مكونات/صورة صنف/حذف طلب/إخفاء-إظهار/إدارة أصناف (رفع ملفات؛ حارس ملف-فارغ)
│   ├── admin/dashboard/page.tsx        # قائمة الأصناف: Drag & Drop ترتيب + إضافة/تعديل/إخفاء/إظهار/حذف (بوابة: غير مصادق → /admin)
│   ├── admin/categories/[slug]/page.tsx# صفحة صنف مستقلة: لوحة إدارة الصنف + صورة + أطباق بأقسام أكورديون (بوابة: غير مصادق → /admin)
│   └── admin/orders/page.tsx           # قائمة طلبات الزبائن (بوابة: غير مصادق → /admin)
├── components/
│   ├── menu-ui.tsx                     # Hero, بطاقة صنف مربعة (aspect-square)، بطاقة منتج (تُطلق أنيميشن الطيران للسلة) + Bottom Sheet لتخصيص المكونات (قسم «المكونات» المشمولة / «الإضافات» الاختيارية، إلزامية 🔒)
│   ├── cart.tsx                        # CartProvider (فلاينج إيمج + نبضة 🛒 + إشعار) + SiteHeader (زر السلة مرجع الطيران) + درج السلة (تأكيد طلب → orders)
│   ├── admin-theme.tsx                 # يبدّل نطاق `html.admin-light` لصفحات /admin (واجهة ادمن فاتحة دون لمس الزبون)
│   └── admin-ui.tsx                    # نماذج/جداول الادمن (useActionState) + Reorder للأصناف (Drag & Drop) + لوحة إدارة صنف + أكورديون أطباق (معلومات/مكونات/إضافات/خيارات أخرى) + لوحة رؤية (إخفاء/إظهار) + الطلبات
├── lib/
│   ├── db.ts                           # طبقة بيانات async مزدوجة: node:sqlite محلياً / Turso (TURSO_URL) — schema idempotent + ترحيل imageUrl/isRequired/isHidden + CRUD صنف (create/update/delete/reorder) + إخفاء/إظهار جماعي
│   ├── upload.ts                       # رفع صور من الجهاز: تحقق صيغة/حجم (حد 4MB) → public/uploads
│   ├── session.ts                      # HMAC sign/verify + verifyPassword (HttpOnly)
│   ├── cart.ts                         # منطق سلة نقي: count/total/formatOrderLine + flyVector (متجه طيران العنصر إلى السلة) (قابل للاختبار)
│   ├── logger.ts                       # تسجيل async غير حظري: DEBUG/INFO/WARN/ERROR → logs/app.log
│   └── utils.ts                        # RESTAURANT_NAME + تنسيق السعر (CURRENCY دج من env)
├── scripts/
│   ├── seed.ts                         # 4 أصناف بصور + 19 منتج + مكونات أمثلة (idempotent)
│   └── smoke.ts                        # تحقق آلي: بيانات + CRUD + مكونات + طلبات + سلة + مصادقة + CRUD صنف/ترتيب/حذف متسلسل
├── data/                               # menu.db (SQLite) — مُستبعد من git
├── logs/                               # app.log — مُستبعد من git
├── public/
└── PROJECT_MAP.md
```

مبادئ: تقسيم حسب النطاق (Domain-Driven)؛ `lib/` فقط للمنطق المتكرر فعلياً؛ لا Micro-files؛ لا مسارات API — Server Actions فقط؛ Zero feature creep.

**Schema (SQLite):**
- `Category`: id, slug(unique), nameAr, icon(emoji), imageUrl, **isHidden(0/1 — مخفي من منيو الزبون)**, sortOrder
- `Product`: id, categoryId(FK→Category ON DELETE CASCADE), name, description, priceCents(Int), imageUrl, isAvailable(0/1), **isHidden(0/1 — مخفي من منيو الزبون)**, sortOrder
- `Ingredient`: id, productId(FK→Product ON DELETE CASCADE), name, priceCents, isExtra(0 أساسي/1 إضافة بسعر), **isRequired(0/1 إلزامي لا يُزال)**, sortOrder
- `orders`: id, items(TEXT=JSON array من بنود منسّقة), totalCents, createdAt

## [ORPHANS & PENDING]

- [x] **Node.js 24 LTS** — مثبّت (winget, 24.19.0).
- [x] **M0 Scaffold** — Next 16.3.0 + TS + Tailwind 4.3.3 + Motion 13.
- [x] **M1 Data layer** — schema idempotent + seed (4 أصناف / 19 منتج).
- [x] **M2 مسار الزبون** — `/` → `/menu` → `/menu/[slug]` (200 بتحقق HTTP؛ 404 للصنف المجهول).
- [x] **M3 السلة** — Context + localStorage + درج متحرك (تحقق آلي للمجاميع/النص).
- [x] **M4 لوحة الادمن** — login (HMAC session) + CRUD محمي (بوابة 307/200 + انعكاس فوري مثبت).
- [x] **M5 الصقل** — أنيميشنات Motion + استجابة + تسجيل + `build`/`lint`/`smoke` خضراء.
- [x] **M6 العملة دج** — `CURRENCY` افتراضياً دج (دينار جزائري) + تسميات الادمن.
- [x] **M7 مكوّنات الطبق** — جدول `Ingredient` + نافذة تخصيص للزبون (إزالة/إضافة بسعر) + إدارة من الادمن (اسم/سعر/نوع) + إعادة حساب السعر خادمياً.
- [x] **M8 صور الأصناف** — `Category.imageUrl` (ترحيل ALTER للقواعد القائمة) تُعرض بدل الإيموجي ويُعدّلها الادمن.
- [x] **M9 الطلبات للادمن** — جدول `orders` + `placeOrderAction` + صفحة `/admin/orders` (حذف)؛ واتساب أُزيل كلياً.
- [x] **M10 تذييل إنستغرام** — رابط `https://www.instagram.com/sole_.vibes/?hl=en` في التذييل.
- [x] **M11 رفع الصور من الجهاز** — `lib/upload.ts` (تحقق صيغة/حجم، حد 4MB، حفظ في `public/uploads`) + `serverActions.bodySizeLimit: "5mb"`؛ نماذج الادمن (طبق/صنف) أصبحت `type="file"` بدل روابط، مع معاينة عند التعديل.
- [x] **M12 أصناف بمكونات إلزامية وصفحات مستقلة** — لوحة الادمن أصبحت شبكة أصناف → `/admin/categories/[slug]` لكل صنف (صورة + أطباق + مكونات)؛ أقسام منفصلة للأساسية/الإضافات؛ `isRequired` يمنع إزالة المكون لدى الزبون (🔒) ويُستثنى من `removedList`.
- [x] **M13 نافذة تخصيص أوسع وأوضح** — `ProductCustomizer` من `max-w-lg` إلى `max-w-2xl`؛ قسم أساسية/إضافات جنباً إلى جنب على الشاشات ≥sm (شبكة شرطية عند وجود القسمين فقط)؛ شرائح/صفوف/عناوين/إجمالي أكبر؛ بلا تغيير في منطق التخصيص.
- [x] **M14 نشر حقيقي: GitHub + Turso + Vercel** — المستودع على GitHub (`oubchirmohamedsaid-code/restaurant-menu`، فرع `main`)؛ قاعدة Turso `restaurant-menu` (URL+Token في `.env` المحلي فقط، gitignored)؛ تُحقّق Turso باختبار HTTP كامل (كل المسارات 200) وسموك خضراء. **النشر الحي يعمل والـ auto-deploy من GitHub مُثبَت** (اختبار شارة v1.0).
- [x] **M14b الصور السحابية (Vercel Blob)** — إضافة طبق/تعديل في الادمن كان يكسر الموقع على Vercel لأن `public/uploads` للقراءة فقط؛ الآن `saveImageUpload` يرفع إلى **Vercel Blob** تلقائياً عند وجود `BLOB_READ_WRITE_TOKEN` (مرتبط تلقائياً بالمشروع من Storage)، مع بقاء الحفظ المحلي احتياطياً، وحذف الصورة القديمة عند الاستبدال/الحذف. أُضيف `*.public.blob.vercel-storage.com` إلى `images.remotePatterns`.
- [x] **M15 إخفاء/إظهار المنتجات + واجهة ادمن فاتحة** — عمود `isHidden` مستقل عن `isAvailable` (ترحيل idempotent على Turso والمحلي)؛ في صفحة الصنف: زر «إخفاء المنتجات غير المتوفرة» (يبحث عن كل طبق `isAvailable=0` غير مخفي، تأكيد بالعدد، رسالة نجاح/لا-يوجد) + «إظهار المنتجات المخفية» (اختيار يدوي بمربعات). منيو الزبون يفلتر `isHidden=1` و`placeOrderAction` يرفضها. الثيم الفاتح للادمن عبر `html.admin-light` (يتجاوز CSS vars) يبدّله `AdminTheme` (usePathname) — بلا مساس بواجهة الزبون.
- [x] **M16 بطاقات مربعة + إدارة أصناف كاملة + أطباق بأقسام أكورديون** — (1) بطاقات أصناف مربعة `aspect-square` للزبون والأدمن، والاسم ظاهر دائماً فوق الصورة؛ (2) هاتف الزبون = `grid-cols-2` بالضبط ثم `sm:3`/`lg:4`؛ (3) إدارة أصناف كاملة في الأدمن: إضافة (اسم+أيقونة+صورة اختيارية، slug مولّد آلياً) / تعديل اسم / تغيير صورة / إخفاء/إظهار (`Category.isHidden` جديد بترحيل idempotent) / حذف متسلسل (ينظّف صور الأطباق والصنف ثم يعيد للتوجيه للوحة) / ترتيب Drag & Drop عبر `Reorder` من motion (أزرار حفظ الترتيب عند التغيير)؛ (4) صفحة تعديل الطبق صارت أقسام أكورديون متحركة (`AnimatePresence`): «معلومات الطبق» مفتوح + «المكونات»/«الإضافات»/«خيارات أخرى» مغلقة؛ مكونات وإضافات لكل طبق بمعزل تام؛ (5) تعديل السعر دون رفع صورة (حقل صورة اختياري + حارس «ملف فارغ = لا رفع» في `uploadImageField`) مع إبقاء الصورة الحالية؛ (6) حفظ جزئي آمن (UPDATE موجه بالـ id فقط، لا يمسح شيئاً) + حالة حفظ «تم الحفظ ✓» + تعطيل مزدوج الإرسال عبر `pending`. الزبون: الصنف المخفي يغيب من `/menu` و404 في `/menu/[slug]`.
- [x] **M17 أنيميشن الإضافة للسلة + Bottom Sheet للمكونات** — (1) **إضافة فورية لا يُؤخّرها الأنيميشن**: الضغط على «أضف إلى السلة» يستدعي `add()` ثم يُطلق **فلاينج إيمج** من صورة الطبق إلى زر السلة (مسار ديناميكي عبر `getBoundingClientRect` بلا إحداثيات ثابتة، `flyVector` نقي + اختبار)؛ عند الوصول: نبضة أيقونة 🛒 (`key={bumpNonce}`) + إشعار «أُضيف [الاسم] إلى السلة» يختفي ذاتياً. فشل الحساب (عنصر مخفي/صفر) → نبضة+إشعار مباشر؛ إضافات سريعة متعددة → رحلات مستقلة بـ id. (2) زر «المكونات والإضافات» يفتح **Bottom Sheet** (هاتف: `items-end` + مقبض سحب؛ كمبيوتر: Modal موسّط بـ `sm:items-center`) بحركة انزلاق لأعلى وغلاق معاكس (`AnimatePresence`)، جسد قابل للتمرير + تذييل ثابت (الإجمالي ينبض عند التغير + زر التأكيد يُطلق أنيميشن الطيران أيضاً) — دون تمدد بطاقات الشبكة. (3) فصل بصري واضح: قسم **«المكونات»** بشارة «مشمولة في السعر» وصفوف بقائمة نقاط (مشمول ✓ / محذوف ✗، الإلزامي 🔒 مقفل) وقسم **«الإضافات»** بشارة «اختياري» ومربعات اختيار مخصصة + تظليل `border-accent bg-accent/15` عند الاختيار (اختيار متعدد).
- [x] **M18 أنيميشن طيران أوضح وأبطأ** — ثوابت Shared/Core في `lib/cart.ts`: `FLY_TARGET_OPACITY = 1` (الطبق يبقى **معتماً تماماً** طوال الرحلة — لا تلاشٍ عند الوصول) + `FLY_TRANSITION = { duration: 1.3, ease: "easeInOut" }` (حركة **أبطأ** من الـ spring السابق ~0.7s وتنعيم بداية/نهاية يجعل العين تتبع الطبق حتى زر السلة) + `FLY_TARGET_SCALE = 0.08` (التقلّص عند الوصول كما كان). `FlyingImage` في `components/cart.tsx` يستخدم الثوابت بدل القيم الحرفية. **Logging**: غير ممكن هنا — الإعداد Client-side بحت (لا وصول لـ `lib/logger.ts` الذي يكتب لملف الخادم)؛ القرار موثّق بدل ذلك (انظر سجل القرارات).
- [x] **M19 أنيميشن الطيران مرحلتان: صعود ناعم ثم انطلاق منحنٍ سريع** — عوّضت `FLY_TRANSITION` (الخط المستقيم) بإعداد مرحلتين في `lib/cart.ts`: (1) **الصعود**: `y: 0 → -FLY_RISE` (150px) بمدة 0.85s و`ease [0.65,0.05,0.36,1]` (easeInOutCubic — صعود بطيء كالطفو ثم توقّف ناعم)، (2) **الانطلاق**: `x: 0→dx` و`y: -FLY_RISE→dy` بمدة 0.5s لكن بإيزنغ **مختلف لكل محور** (أفقي `[0.16,1,0.3,1]` easeOut يتحرك بسرعة ثم يتباطأ، رأسي `[0.5,0,1,0.5]` easeIn يتسارع لاحقاً) — اختلاف الإيزنغ بين المحورين يولّد **مساراً منحنياً (قوساً)** لا خطاً مستقيماً؛ الـ `scale` له نفس easeIn فيتقلّص **قرب السلة فقط**. الصورة **معتمة تماماً** (`opacity=1`) بلا أي Fade. التتابع عبر `onAnimationComplete` → `setLaunched(true)` في `FlyingImage` (لا مكتبات جديدة، منطق السلة/النبضة/الإشعار دون تغيير). **Logging**: غير ممكن (Client-side بحت) — موثّق هنا.
- [x] **M20 الطيران: تصغير 50% بدل الصعود + حواف سوداء** — استُبدل صعود M19 بالتصغير (في مكان الصورة) في المرحلة الأولى: `scale: 1 → FLY_PREP_SCALE (0.5)` بمدة 0.85s و`ease [0.65,0.05,0.36,1]` (نفس الإيزنغ الناعم للصعود) بلا أي حركة رأسية (المرحلة الثانية تبدأ من 0 وتنطلق بالقوس كما في M19: `x: 0→dx`/`y: 0→dy` بمدة 0.5s بإيزنغ متباين للمحورين و`scale: 0.5 → FLY_TARGET_SCALE`). أُزيل `FLY_RISE` (يتيم) واستُبدل `FLY_RISE_TRANSITION` بـ `FLY_PREP_TRANSITION` بنفس القيم. الصورة الطائرة أصبحت **بحواف سوداء واضحة**: استُبدل `ring-1 ring-white/10` بـ `border-2 border-black` (مع بقاء `shadow-2xl` و`rounded-2xl`) لتبدو واضحة فوق أي خلفية. الشفافية (`opacity=1`) والنبضة والإشعار كما هي. **Logging**: غير ممكن (Client-side بحت) — موثّق هنا.
- [x] **M21 أساس نظام إدارة الطلبات: دورة حياة + سجل نشاط + إعدادات** — وسّع جدول `orders` بأعمدة المراحل والأولوية والدفع والعميل الاختياري (نقلاً مسبقاً للاتجاه الذي تسمح به التوسعة) مع جدولين جديدين: `order_line` (بنود منظمة `productId/name/qty/unitCents/lineCents/extras/removed` لتحليلات المنتجات) و`order_activity` (سجل النشاط `at/actor/action/detail` لكل حدث). جدول `settings` (key/value) يخزّن عتبات التأخير `late_new_minutes=15`/`late_preparing_minutes=30`/`late_delivered_minutes=20` (تُزرع تلقائياً). واجهة البيانات الجديدة: `createOrder(lines,totalCents,opts)` (تُنشئ البنود + تسجل «تم إنشاء الطلب»)، `getOrder(id)` (تفاصيل+بنود+نشاط)، `updateOrderStatus` (تسجل الطوابع الزمنية `confirmedAt/preparingAt/deliveredAt/completedAt/cancelledAt` + `updatedAt` + أحداث النشاط لكل انتقال، مع `reason` للإلغاء)، `setOrderPriority`، `setOrderPaymentStatus`، `getSetting`/`setSetting`. `placeOrderAction` يمرر بنوداً منظمة بدل نصوص العرض (الـ JSON العرضي ما يزال يُولَّد خادمياً عبر `formatOrderLine`). النطاق النقي `lib/orders.ts`: حالات/أولويات/أسباب الإلغاء/تسميات عربية + `minutesInStage`/`isLate` (اكتشاف المتأخر بالعتبات القابلة للضبط — قابلة للاختبار). الترحيلات اندمجت في `migrate()` مشتركة (DRY بدل كتلتين متطابقتين). **Logging**: الخادم محتفظ به (طلبات إدارية) لكن سجل النشاط لكل طلب أصبح في `order_activity`. اختيار المستخدم: **بدون بيانات زبون حالياً** (الأعمدة موجودة وفارغة — تُجمع لاحقاً)، **أدمن واحد + توقيع النشاط** (الموظفون/الصلاحيات مؤجلة)، **رسوم SVG يدوية بلا مكتبات**.
- [x] **M23 لوحة الطلبات Kanban + Sidebar** — صفحة `/admin/orders` صارت خط أنابيب بصرياً: 4 أعمدة (طلب جديد → قيد التحضير → تم التوصيل → مكتمل) كل عمود بعنوان وعدّاد + قسم «ملغى» منفصل أسفله. البطاقة تعرض: `#id`، شارة الأولوية (عادي/مهم/عاجل)، الوقت، الإجمالي، عدد الأصناف، **مدة البقاء في المرحلة** (`منذ N د`) وعلامة ⚠️ حمراء عند تجاوز عتبة التأخير (من `settings` عبر `isLate`) مع ترتيب الأقدم أولاً ليصعد المتأخر. إجراءات سريعة حسب المرحلة (تأكيد→تحضير، تم التوصيل، إكمال، إلغاء مع **نافذة أسباب إلغاء** من الستة + تسجيل السبب). بحث برقم الطلب أو نص الطبق + فلاتر (أولوية + «متأخرة فقط»). إضافة `app/admin/layout.tsx` + `admin-shell.tsx`: **Sidebar** (شاشات كبيرة) / شريط علوي (هاتف) مع تسمية النشط وحالة تسجيل الخروج — يتوسع بروابط لاحقاً. استُبدل `OrdersView` القديم و`DeleteOrderButton` و`deleteOrderAction` (صارت يتيمة). تحديث تلقائي عبر `router.refresh()` كل 30 ثانية. **Logging**: العمليات الإدارية مسجّلة خادمياً (`order confirmed/delivered/completed/cancelled`).
- [x] **M24 تفاصيل الطلب: Panel + Timeline + سجل نشاط + إعادة + طباعة** — النقر على أي بطاقة (أو سطر ملغى) يفتح **لوحة جانبية** (حركة spring + إغلاق بـ ✕) تعرض: البنود المنظمة (مع `+ الإضافات`/`بدون` و`سعر البند`)، تفصيل المجموع (المجموع/التوصيل/الخصم/الإجمالي)، **مسار زمني** (طلب جديد → تم التأكيد → قيد التحضير → تم التوصيل → مكتمل/ملغى) بوقت كل نقطة ونقطة ملونة للمرحلة، **سجل النشاط** (وقت + التسمية العربية + التفصيل + الموقّع)، اختيار الأولوية وتبديل حالة الدفع من اللوحة مباشرة، وزر **إعادة الطلب** للمكتمل (ينشئ طلباً جديداً بنفس البنود عبر `reorderOrderAction` — يسجل «تم إنشاء الطلب» تلقائياً). **الطباعة**: زر يفتح `/print/[id]` في تبويب جديد (صفحة مستقلة خارج layout الإداري تحرسها `isAdmin()`) تعرض فاتورة نظيفة وتطبع تلقائياً؛ قواعد `@media print` في globals.css تخفي كل شيء عدا `.print-area` (تقنية visibility) بلا تعديل الترويسة/التذييل. أُضيف `getOrderDetailAction` (إرجاع `OrderDetail` من الخادم). **Logging**: `order reordered` في السجل الخادمي؛ نشاط الطلب نفسه في `order_activity`.
- [x] **M25 الإشعار الفوري (SSE) + تنبيه صوتي** — صفحة `/admin/orders` تشترك في **Server-Sent Events** عبر `/admin/orders/events` (Route Handler `text/event-stream`، `runtime: nodejs`، محروساً بالجلسة — 401 بلا كوكي). الخادم يستقصي `orders` كل 4 ثوانٍ ويرسل حدث `new_order` (id/إجمالي/وقت) لأي طلب جديد يتجاوز `lastId`، مع نبض `: ping` كل 20 ثانية لإبقاء الاتصال وحدث `init` فوري عند الفتح (لأن Next يحتجز ترويسة الاستجابة حتى أول بايت) و`AbortController`/`request.signal` يغلق الدفق عند قطع الزبون. في العميل: `components/order-alert.tsx` يفتح `EventSource`، وعند `new_order` يعزف **جرساً ثنائي النغمة** عبر Web Audio (بلا ملف صوتي) ويظهر **شريط «طلب جديد #N»** (يتلاشى بعد 7 ثوانٍ) ثم `router.refresh()` فيصل الطلب للوحة فوراً — دون انتظار تحديث الـ 30 ثانية. `print:hidden` على الشريط. **Logging**: لا جديد (الاستقصاء يقرأ فقط)؛ لا توجد كتابة خادمية متكررة.
- [x] **M26 صفحة الإعدادات** — `/admin/settings` (رابط جديد ⚙️ في الـ Sidebar): نموذج `SettingsForm` (useActionState) يعدّل **عتبات تأخير مراحل الطلب** الثلاث (`late_new_minutes`/`late_preparing_minutes`/`late_delivered_minutes`) المستخدمة أصلاً في لوحة الطلبات كعلامة ⚠️ — فتتغير القيم فوراً من الواجهة بلا تعديل كود. الصفحة تقرأ القيم الحالية (`getSetting` مع تراجع `DEFAULT_LATE_MINUTES`) و`saveSettingsAction` يتحقق (أعداد صحيحة 1–720) ويكتب عبر `setSetting`؛ لا ثوابت مكررة (المفاتيح معرّفة في actions). **Logging**: `settings saved` مع القيم. البنية قابلة للتوسّع: أي إعداد مستقبلي = مفتاح `settings` + حقل نموذج.
- [x] **M27 لوحة الإحصائيات برسوم SVG يدوية** — `/admin/stats` (رابط 📊): خمس بطاقات KPI (طلبات اليوم/إيراد اليوم/طلبات نشطة/غير مدفوعة/متوسط قيمة الطلب) + **منحنى الطلبات** و**منحنى الإيراد** لآخر 7 أيام (SVG `viewBox` responsive: مساحة متدرجة + خط + نقاط + تسميات أيام، بلا أي مكتبة رسوم) + **Donut** توزيع الطلبات حسب الحالة (أقواس `stroke-dasharray` متراكمة بدوران −90°، ألوان ثابتة لكل حالة + وسيلة إيضاح) + **أطباق أكثر طلباً** (أشرطة أفقية SVG حسب الكمية) + عدادات الأولويات. كلها **Server Components** (SVG ثابت بلا حالة). المصدر `lib/stats.ts` **نقي** (`computeStats(orders, lines)` يجمع: إيراد يستثني الملغى، نشطة = new+preparing+delivered، يوم من `createdAt`، أعلى المنتجات من `order_line` عبر `listAllOrderLines`) — **قابل للاختبار** في smoke (KPIs + أيام + حالة + منتجات). يتعامل مع البيانات الفارغة (مقسومات حدّها الأدنى 1 + رسالة «لا توجد طلبات»). **Logging**: لا جديد (قراءة فقط).
- [x] **M28 تصدير CSV للطلبات** — رابط «تصدير CSV» في شريط أدوات لوحة الطلبات يفتح `/admin/orders/export` (Route Handler محروس بالجلسة — 401 بلا كوكي) فينزّل ملف `orders.csv` (أو `orders-<status>.csv` عبر `?status=`) بعمودين قابلين للقراءة بجلسة Excel عربية: الرقم/التاريخ/الحالة/الأولوية/البنود/الإجمالي/التوصيل/الخصم/الدفع/اسم الزبون/هاتفه/عنوانه/ملاحظات/سبب الإلغاء — القيم العربية عبر `STATUS_LABELS`/`PRIORITY_LABELS`، الأرقام بدقتين عشريتين، ترشيح `"` في النصوص، و**UTF-8 BOM** (`\uFEFF`) مسبوقاً حتى يفتح Excel العربية بلا تشوّه (التحقق: 200 + `text/csv` + `attachment; filename` + أول بايت BOM + 401 غير مصادَق). **Logging**: لا جديد.
- [x] **M29 لوحة الطلبات DnD احترافية + دفع تلقائي + إحصائيات تفاعلية** — (1) **تلميع بصري**: كلسز `shadow-soft`/`shadow-card` مخصصة أُضيفت عبر `@theme` (ظلال ناعمة/كروتية تتبع الثيم، بقيم أفتح في وضع `admin-light`)، بطاقات الطلبات `rounded-2xl border-line bg-surface shadow-soft hover:shadow-card`، رؤوس الأعمدة `rounded-3xl bg-card-2/70 shadow-soft`، الأزرار/الحقول `rounded-xl` مع `focus:ring-accent/20`؛ تُطبَّق نفس الظلال على بطاقات الإحصائيات (`KpiCard`) وصناديق `/admin/stats`. (2) **سحب وإفلات بين الأعمدة** عبر **@dnd-kit** (قرار: `@dnd-kit/core@6.3.1` + `sortable@10.0.0` + `utilities@3.2.2` — لأن `motion/Reorder` أحادي القائمة ولا يعبر الأعمدة؛ `motion` يبقى للأنيميشن): `DndContext` + `PointerSensor` (`activationConstraint: distance=6` فيدعم اللمس والفأرة) + `closestCorners`؛ أعمدة `useDroppable` (توهّج عند `isOver`) وبطاقات `useSortable` (ترانسفورم CSS، `touch-none`) مع `DragOverlay` (بطاقة شفافة أثناء السحب، إسقاط 200ms)؛ `onDragOver` يعيد ترتيب البطاقات داخل العمود/ينقل بين الأعمدة (`arrayMove` + `splice`)، `onDragEnd` يستدعي `setOrderStatusAction` عند تغيّر العمود فقط ثم يعيد ترتيب العمود؛ الإفلات خارج أي عمود ⇢ إعادة بناء من الخادم؛ حارس نقرة (`suppressClickRef`) يمنع فتح لوحة التفاصيل بعد الإفلات؛ الإلغاء (**cancelled**) يبقى خارج نطاق السحب. (3) **دفع تلقائي عند الإكمال**: عمود `paidAt INTEGER` جديد في `orders` (ترحيل idempotent في `migrate()`)؛ `updateOrderStatus` عند الوصول إلى `completed` يضبط `paymentStatus=paid` + `paidAt=now` ويسجّل «تم الدفع تلقائياً عند الإكمال»، وعند **الإرجاع من المكتمل** لمرحلة سابقة يعيد `unpaid` + `paidAt=null` ويسجّل «أُلغي الدفع عند الإرجاع»؛ `setOrderPaymentStatus` يضبط `paidAt` عند الدفع. `OrderDetailPanel` يعرض «وقت الدفع». (4) **إجراء حركة عام للـ DnD**: `setOrderStatusAction(orderId, status)` في `app/admin/actions.ts` (يحرس `isAdmin()` ويسجّل `order status moved`) — إجراءات التأكيد/التوصيل/الإكمال السريعة تبقى منفصلة. (5) **إحصائيات تفاعلية**: `components/stats-charts.tsx` صار Client Component بخط تمرير عمودي (rect) على منحنيَي الطلبات/الإيراد يعرض **Tooltip** (تسمية اليوم + القيمة) + خط إرشادي متقطع وتكبير نقطة المعلّق؛ `RevenueChart` انتقل إلى الملف المذكور؛ **الأطباق الأكثر طلباً** أُعيد تصميمها قائمة HTML (وسام ترتيب، صورة المنتج عبر `next/image` عند توفرها من `listAllProducts()`، اسم + كمية + إيراد + شريط نسبة) بدل أشرطة SVG — عبر إضافة `productId` إلى `TopProduct` في `lib/stats.ts` (مصدره `order_line.productId`). **Logging**: `order status moved` في السجل الخادمي. **قرار**: DnD يغطي أعمدة النشاط الأربعة فقط؛ قسم «ملغى» ثابت.
- [x] **M30 حاوية كروتية للوحة الطلبات على الحاسوب** — تطبيق الطلب السابق («الحواف + كل شيء داخل بطاقته») على مقاس **الكمبيوتر** أيضاً: في `components/orders-pipeline.tsx` حُفظت لوحة السحب (DndContext) والقسم الملغى داخل حاوية `lg:rounded-3xl lg:border lg:border-line lg:bg-card lg:p-4` — تفعّل فقط عند `lg`+ (desktop) فتحوّلت اللوحة (التي كانت تطفو على خلفية الصفحة) إلى **بطاقة واحدة تحتضن بطاقات الأعمدة** مطابقةً لإحساس الجوال، بينما **الجوال يبقى كما هو** (بلا تغيير). أُعيدت محاذاة الأعمدة/البطاقات (DndContext + DragOverlay) تحت الحاوية. لا كلاسات `max-*` (تتبع قاعدة المشروع base + sm/md/lg). **Logging**: لا جديد (تغيير عرض فقط). **تحقق**: eslint نظيف + `next build` نظيف + smoke كامل أخضر.
- [x] **M31 تلميع Desktop للوحة الطلبات (سبب «المساحة البيضاء» + بطاقات/أعمدة واضحة)** — **جذر المشكلة**: ثيم `admin-light` كلّه قريب من الأبيض (الصفحة `#faf9f6`، الأعمدة `bg-card-2/70` ≈ `#f5f4f1` تكاد تختفي فوق الأبيض، البطاقات `#ffffff`، الحدود `#e7e4dc` باهتة، والظلال شبه معدومة)؛ على Desktop تتمدّد الأعمدة (`lg:w-auto`) فتصبح مساحات بيضاء مستوية («مساحة بيضاء واحدة»)، بينما على الهاتف أعمدة `w-72` الضيقة تُقرأ كصناديق منفصلة. **اكتشاف إضافي**: كلاسات `shadow-soft`/`shadow-card` في Tailwind v4 **تُضمّن قيمة `--shadow-soft` الداكنة** في الـ utility، لذلك إعادة تعريف `html.admin-light { --shadow-soft: … }` في `globals.css` **كود ميت** (لا يؤثر). **الإصلاح (Desktop فقط عبر `lg:`)** في `components/orders-pipeline.tsx`: حاوية اللوحة `lg:bg-card lg:border lg:border-line lg:rounded-3xl lg:p-4` + ظل دافئ فاتح `lg:shadow-[0_1px_2px_rgba(31,41,55,0.08),0_6px_18px_rgba(31,41,55,0.08)]`؛ أعمدة المراحل `lg:bg-card-2` (صلب `#f3f1ec` فتميّزت عن الأبيض) + `lg:p-3.5` + نفس الظل الفاتح؛ بطاقات الطلبات `lg:p-4` + ظل أعمق قليلاً `lg:shadow-[0_2px_4px_rgba(31,41,55,0.10),0_8px_20px_rgba(31,41,55,0.10)]` + hover احترافي `lg:hover:-translate-y-0.5` (مع `hover:shadow-card` القائم)؛ قسم الملغى `lg:bg-card-2`. **الجوال بلا تغيير، والإحصائيات لم تُلمس**. الـ DnD بالماوس مؤكّد بالكود (`PointerSensor activationConstraint distance=6` + `onDragEnd` → `setOrderStatusAction` + إشارة العمود `isOver`/الواصلة المتقطعة). **تحقق**: eslint نظيف + `next build` أخضر + smoke كامل أخضر + تحقق من أن الـ CSS المبنية تحتوي كل الكلاسات والقيم (مثل `#1f293714`/`#1f29371a`). **مؤقَّت/بيئة**: اتصال Turso من جهاز المطوّر متقطّع الآن (فحص 4 مرات: فشل واحدة؛ الخادم يعيد 500 كلما انتهت مهلة 10s) — بيئي وليس من التعديل؛ أُجل التحقق النهائي HTTP 200 حتى يستقر الاتصال.
- [x] **MD1 OGTX Desktop — الهيكل** — مشروع فرعي مستقل `desktop/` داخل نفس المستودع (Electron 43.3.0 + electron-vite 5 + React 19 + Tailwind 4.3). نافذة frameless 1280×800 (min 980×640) + TitleBar مخصص (minimize/maximize/close عبر IPC) + RTL + خط Cairo (`@fontsource/cairo`) + CSP صارم. `preload` بـ `sandbox:true`/`contextIsolation` وواجهة `OgtApi.call<T>` عامة (لا `nodeIntegration`). وضع `--smoke` يجري اختبار ذاتي عند الإقلاع ويستبدل تلقائياً بقاعدة sqlite محلية مؤقتة بلا لمس Turso الحقيقي (يحذف دليله أولاً — كل تشغيلة نظيفة).
- [x] **MD2 أمان الهوية (desktop)** — جدول `users` (id/fullName/username UNIQUE/passwordHash/role/createdAt/updatedAt) أُضيف إلى SCHEMA في `lib/db.ts` ويُطبَّق idempotent على Turso والمحلي؛ `lib/users.ts` (CRUD + `toPublic` لا يفصح عن hash)؛ `lib/perms.ts` (مصفوفة: OWNER=كل الأقسام+إدارة مستخدمين، ADMIN=كل الأقسام بلا إدارة، EMPLOYEE=الطلبات فقط) + `lib/roles.ts` نقي (لا يستورد db ليعمل في الـ renderer)؛ كلمات المرور **scrypt** (N=16384, r=8, p=1, salt 16B) في العملية الرئيسية + جلسة كائن في الذاكرة؛ إضافة/تعديل/حذف مستخدم تتطلب دائماً تحقق كلمة مرور المالك؛ `RpcError` بأكواد موحدة (UNAUTHORIZED/FORBIDDEN/VALIDATION/NOT_FOUND). اختبارات `scripts/test-{auth,perms,orders}.mts` خضراء.
- [x] **MD3 الشاشات + IPC** — Boot (فحص اتصال DB + زر إعادة محاولة) → Hero (يدخل setup أو login حسب phase) → Setup (إنشاء OWNER أول مرة فقط) → Login (دخول + زر «+ إضافة مستخدم» بتحقق المالك) → Dashboard. كل عمليات IPC محروسة بحراسة صلاحيات في **main process** (لا تُؤتمن الواجهة).
- [x] **MD4 Dashboard + Sidebar + إعدادات** — لوحة تحكم بـ Sidebar من 8 أقسام (orders/cashbox/menu/stock/stats/expenses/suppliers/settings) مفلترة حسب صلاحيات المستخدم مع `PlaceholderScreen` لكل قسم غير المبني؛ قسم الإعدادات وظيفي (OWNER فقط): قائمة مستخدمين + إضافة + تغيير دور + حذف مع تحقق المالك، مع حمايتَي «لا حذف/تغيير المالك الأخير» و«لا تغيير دور حسابك الحالي».
- [x] **MD5 قسم الطلبات الوظيفي** — `OrdersSection` يقرأ طلبات **Turso الحقيقية** (نفس قاعدة موقع الزبون عبر `lib/orders`) من العملية الرئيسية: بطاقات بالحالات مع عدّادات ملخص، تفاصيل الطلب قابلة للطي، تحويل الحالة `new→preparing→delivered→completed`/`cancelled`، تحديث تلقائي كل 8 ثوانٍ + زر تحديث يدوي.
- [x] **MD6 التغليف والتثبيت** — `typecheck` نظيف (tsc node+web) + الاختبارات الثلاثة PASS + **smoke PASS** (تسلسل setup→login→perms→orders عبر المعالجات الحقيقية) + النافذة تفتح بلا أخطاء (`--enable-logging` نظيف) + `electron-builder` أنتج `release\OGTX Setup 0.1.0.exe` (NSIS، لغة 1025 عربي، x64) والنسخة `release\win-unpacked\OGTX.exe` تعمل. إصلاح مهم: `DATA_DIR`/`LOGS_DIR` كانت تُلتقط عند تحميل الوحدة فأصبحت قراءتها كسولة لاحترام `OGT_DATA_DIR` (سمح بجعل smoke يعمل على قاعدة مؤقتة). إصلاح ثانٍ: `lib/orders.ts` أصبح مستقلاً عن `./db` (بنيته المحلية) كي يمر فحص الواجهة بلا جرّ عقد `node:*`.
- [x] **MD7 لوحة الطلبات Kanban (المرحلة الثانية)** — استبدال `OrdersSection` بقسم Kanban احترافي على نفس **Turso** (لا بيانات وهمية/نظام منفصل): (1) **أعمدة المراحل الخمس** (جديد/قيد التحضير/تم التوصيل/مكتمل/ملغى) مع عدّاد لكل عمود + بطاقة (رقم، اسم/هاتف الزبون، معاينة البنود، الوقت + دقائق في المرحلة + شارة «متأخر» من `stageStartTs`+عتبات الإعدادات، شارة الدفع، شارة الأولوية، الإجمالي). (2) **سحب وإفلات** بنفس بنية لوحة الويب (`DndContext` + `PointerSensor` مسافة 6 + `closestCorners` + `useDroppable`/`useSortable` + `DragOverlay`)؛ الإفلات بين الأعمدة يستدعي `updateOrderStatus` (المعتمد أماناً) مع تحسين متفائل فوري وعودة من الخادم عند الخطأ؛ البطاقات الملغاة غير قابلة للسحب. (3) **تفاصيل الطلب** لوحة جانبية: الزبون + أزرار اتصال/نسخ/واتساب (عبر `app:openExternal` المقيّد بـ http/https/tel/mailto و`app:copyText` عبر `clipboard` في main)، البنود مع صورة المنتج إن كانت `https` (خريطة من `products:list`)، المجموع/التوصيل/الخصم/الإجمالي، الأولوية والدفع (مدير/مالك فقط)، مسار الطلب Timeline + سجل النشاط `order_activity` بمعرّف الفاعل، سبب الإلغاء. (4) **إلغاء بنموذج** بستة أسباب `CANCEL_REASONS` + سبب مخصص (مدير/مالك فقط). (5) **بحث وفلاتر**: بحث (رقم/اسم/هاتف/بنود) + أولوية + دفع + فترة (اليوم/7أيام) + «المعالج» (من `lastActor` عبر دالة `listOrderSummaries` الجديدة في `lib/db` بضمّ أحدث نشاط) + «متأخرة فقط» + مسح. (6) **إشعارات حيّة**: استقصاء كل 6 ثوانٍ (الأفضل المتوافق مع `node:sqlite`/Turso) يكتشف الطلبات الجديدة عبر `id > lastMaxId` فيُظهر Toast + **صوت** Web Audio (مولّد نغمات بلا ملفات، مُخنّق مرة/8ث، قابل للإيقاف ويُحفظ في localStorage) + **Badge** حمراء بعدد الطلبات الجديدة على أيقونة «الطلبات» في Sidebar (يُحدَّث حتى خارج القسم). (7) **صلاحيات**: `canManageOrders(role)` جديدة في `lib/perms.ts` (OWNER/ADMIN)؛ الإلغاء/الأولوية/الدفع/الرجوع لمرحلة سابقة = مدير/مالك فقط، والموظف يتقدم للأمام فقط — كل التحويلات تُتحقق في main process و`expectDenied` في smoke يثبت منع الموظف. (8) **تحقق**: `typecheck` + الاختبارات (بما فيها `test-perms` الجديد) PASS + **smoke PASS** موسّع (موظف يتحرك للأمام، يُمنع من الإلغاء/الرجوع/الأولوية/الدفع؛ مالك يلغي/يغيّر الأولوية؛ الإكمال يدفع تلقائياً؛ الإلغاء يعيد غير مدفوع) + الإقلاع بلا أخطاء + **no-regression للجذر**: `eslint` نظيف + `next build` أخضر.
- [x] **MD10 قسم المينيو (المرحلة الرابعة)** — قسم «المينيو» وظيفي بالكامل في OGTX Desktop على **نفس قاعدة Turso المشتركة**: (1) **بنية البيانات** — 3 جداول جديدة في SCHEMA بتطبيق idempotent: `addon_groups` (name, isActive, sortOrder)، `addon_options` (groupId FK CASCADE, name, priceCents, sortOrder + `idx_addon_option_group`) و`product_addon_groups` (productId FK CASCADE, groupId FK CASCADE, sortOrder، PK مركّب (productId,groupId) + `idx_product_addon_group_group`) — العلاقة بين الأطباق والمجموعات. (2) **طبقة البيانات `lib/db.ts`** — `setProductHidden`، `reorderProducts(categoryId, ids)`، `listAddonGroupsWithOptions()` (مع `options` و`productCount` لكل مجموعة)، `createAddonGroup`/`updateAddonGroup`/`deleteAddonGroup` (CASCADE)، `addAddonOption`/`updateAddonOption`/`deleteAddonOption`، `listProductAddonGroupIds`/`setProductAddonGroups` (حذف+إدراج مرتب)، و`getMenuSnapshot()` (أصناف ← منتجات ← مكونات/إضافات ← groupIds بترتيب asc). (3) **main process** — 23 معالج `menu*` في `handlers.ts` كلها محروسة بـ `requireMenuManage` (OWNER/ADMIN؛ الموظف `FORBIDDEN`) و`requireSection("menu")`: إنشاء/تعديل/حذف أصناف (حذف الصنف ينظف صور منتجاته)، ترتيب أصناف/منتجات، CRUD منتجات (isAvailable + sortOrder ضمني)، مكونات (isRequired تُحفظ فقط للإضافات حسب قاعدة الموقع)، مجموعات/خيارات إضافات، ربط/استبدال مجموعات بمنتج، رفع صورة عبر IPC `{bytes, originalName}` (Vercel Blob أو `public/uploads` مع حذف القديم بعد نجاح الجديد — أساس «التعديل الجزئي»). القنوات 22 عبر `ipc.ts` ← `preload` ← `window.ogt.menu.*`. (4) **الواجهة** — `MenuSection` (أصناف قائمة جانبية قابلة للسحب DnD بالزر، منتجات قائمة عمودية DnD بالزر مع شارات متاح/مخفي وزرّي تبديل سريعين، تأكيد حذف مخصص) + `CategoryEditorModal` (اسم/أيقونة/صورة «ملف فارغ = لا تغيير»/إخفاء) + `ProductEditorModal` (معلومات الطبق + صورة مع تحذير «اتركها لتعديل باقي الحقول فقط» + محرر مكونات inline أساسي/إضافة/إلزامي + ربط مجموعات إضافات باختيار) + `AddonGroupsModal` (إنشاء/إعادة تسمية/إظهار-إخفاء/حذف مجموعات + خياراتها inline) + `MenuPreviewModal` (معاينة واجهة الزبون: أصناف ظاهرة فقط، أطباق مخفية تستبعد، مكوّنات/إضافات/مجموعات بسعرها، تعتيم «غير متاح»). (5) **تحقق أخضر كامل**: `typecheck` نظيف (node+web) + الاختبارات الخمسة PASS (`test-menu` الجديد يغطي أصناف/منتجات/مكونات/مجموعات/ربط/ترتيب/إخفاء/حذف مع CASCADE و`canManageMenu` في `test-perms`) + **smoke PASS** (يشمل الآن: رفض الموظف لـ`menuSnapshot`/`menuAddonGroups` + دورة owner كاملة إنشاء مجموعة/صنف/طبق/مكوّن/ربط/تحقق snapshot وproductCount ثم تنظيف) + `next build` للويب **بلا ارتداد** (المسارات 13 كلها تُبنى) + `npm run dist` أنتج `release\OGTX Setup 0.1.0.exe`. ملاحظة عرض: صور `http(s)` تعرض مباشرة؛ روابط `/uploads/...` النسبية تعرض أيقونة بديلة (نفس سلوك `CashboxSection`).

- [x] **MD9 تحقق وأرشفة المرحلة الثالثة (صندوق النقود)** — (1) إصلاح: دالة `assert` في `desktop/src/main/smoke.ts` صارت بضميمة اختزال (`asserts cond`) فتمكّن tsc من تضييق النوع بعد الفحص (كان `revTx` يظهر `possibly undefined` رغم `assert(!!revTx, ...)`). (2) **تحقق كامل أخضر**: `npm run typecheck` نظيف (node+web) + الاختبارات الأربعة PASS (`test-auth`/`test-perms`/`test-orders`/`test-cashbox`) + **smoke PASS** (سلسلة setup→login→perms→orders→cashbox: دخل الإيراد عند الإكمال، الاسترجاع المرآتي بعد الإلغاء `direction=out` و`amountCents=7600`، انعدام المبيعات بعد الإلغاء، حذف الطلب) + `next build` للويب **بلا ارتداد** (كل المسارات 13 تبنى، من بينها `/admin/orders/export` و`/print/[id]`) + `npm run dist` أنتج `release\OGTX Setup 0.1.0.exe` (NSIS عربي 1025، x64). (3) درس بيئة: أول محاولة `dist` فشلت بـ `EBUSY ... rmdir release\win-unpacked` لأن **أربع نسخ من OGTX.exe القديمة كانت ما تزال قيد التشغيل** تقفل المجلد — أُنهيت (`Stop-Process`) ثم نجح البناء. بهذا **اكتملت المرحلة الثالثة (صندوق النقود) وتأرشفت**؛ المرحلة الرابعة (قسم المينيو) تليها.
- [x] **MD8 قسم صندوق النقود (المرحلة الثالثة)** — قسم «صندوق النقود» وظيفي بالكامل بدل الـ placeholder: (1) **بنية البيانات** — جدولان جديدان في SCHEMA بتطبيق idempotent: `cashbox_transactions` (txNumber فريد تصاعدي، type=income/expense/adjustment/deposit/withdrawal، direction=in/out منفصلة عن المبلغ حتى تكون الصافي +1000 صالحة، amountCents عدد صحيح، paymentMethod، source=order/manual، orderId/sessionId، userId/userName للفاعل، note، status=active/reversed، correctsTxId ← أصل العملية المصححة، createdAt) و`cashbox_sessions` (openedAt، openingBalanceCents، openedById/Name، status، closedAt، expectedCents/actualCents/diffCents). فهارس: created/session + **فهرس فريد جزئي** `idx_cashbox_order_income_active` على orderId حيث `type='income' AND source='order' AND status='active'` (يضمن idempotency حتى في سباق) + `idx_cashbox_corrects` (تصحيح واحد لكل عملية). (2) **الربط التلقائي بالطلبات** — `recordOrderIncome`/`recordOrderIncomeReversal` تُستدعى من `updateOrderStatus` (إكمال → دخل؛ إرجاع من المكتمل/إلغاء → استرجاع) و`setOrderPaymentStatus` (paid/unpaid) و`deleteOrder` (استرجاع قبل الحذف): الفحص المسبق + الفهرس الفريد يمنعان تكرار الدخل، والاسترجاع يضيف عملية adjustment معاكسة + علامة `correctsTxId` ويحوّل الأصل إلى `reversed`. (3) **الدلالات المالية** — عمليات المعاكسة (mirror بمعرّف `correctsTxId`) **تُستبعد من الرصيد والملخص** (هي سجل تدقيق فقط، وتتعادل مع أصلها الملغى) فتتسق الملخصات مع الرصيد؛ أما سجل العمليات فيعرضها بشفافية. `currentCashboxBalance`: جلسة مفتوحة = افتتاحي + حركة (sessionId=الجلسة أو لاحقة لافتتاحها)؛ آخر إغلاق = `actualCents` + حركة لاحقة مستقلة؛ بلا جلسات = صافي الحركة المستقلة. (4) **الجلسات** — فتح يرفض أثناء فتح أخرى؛ تعديل الرصيد الافتتاحي ممنوع بعد أول حركة إلا للمالك (`role='OWNER'`); إغلاق يحسب `expected = opening + movement` ويحفظ `actual/diff` (قيم سالبة/موجبة). (5) **لا حذف مالي** — التصحيح (`correctCashboxTransaction`) يضيف عملية معاكسة بسببه ولا يُحذف الأصل (قابل للتدقيق). (6) **صلاحيات** — `canManageCashbox` (OWNER/ADMIN) في `lib/perms.ts`؛ الإضافة لكل من يملك القسم، والتصحيح/الإغلاق/تعديل الافتتاحي = مدير/مالك (يُتحقق في main process + `expectDenied` في smoke). (7) **IPC/الواجهة** — 9 قنوات (`cashbox:summary/list/add/openSession/updateOpening/closeSession/sessions/byOrder/correct`) عبر types/preload/handlers/ipc؛ `CashboxSection`: بطاقات ملخص (الرصيد/مبيعات/طلبات مدفوعة/عدد عمليات) + راية جلسة مفتوحة أو آخر إغلاق (مع expected/actual/فرق) + فلاتر (فترة اليوم/أمس/7/30/شهر/سنة/الكل، النوع، الطريقة، المصدر، المستخدم، بحث) + **رسم يومي SVG** بلا مكتبات + جدول عمليات (زر «طلب #id» يفتح لوحة تفاصيل الطلب عبر `cashboxFocus` في DashboardScreen، زر تصحيح لغير المرتبط بالطلبات) + modals (إضافة/فتح/إغلاق/تعديل افتتاحي/تصحيح) + Toasts؛ `OrderDetailPanel` يعرض زر «عرض في صندوق النقود» عند وجود عملية صندوق، و`OrdersSection` يجلبها عبر `cashbox:byOrder`. (8) **تحقق** — `typecheck` نظيف + 4 مجموعات اختبار PASS (أُضيف `scripts/test-cashbox.mts`: دخل واحد عند الإكمال/idempotency/استرجاع عند الإلغاء وإعادة الدفع/مصروف/جلسات/تصحيح/حذف طلب يسترجع دخله/تحقق القيم/فلاتر) + **smoke PASS** (يتضمن دخل الإكمال، إلغاء يلغي المبيعات ويُسجّل الاسترجاع، منع الموظف من القسم) + `next build` للويب بلا تراجع + `dist` (مثبّت NSIS) نجح. **نطاق لاحق**: فتح تلقائي للجلسة من واجهة «الرصيد الافتتاحي»، تقارير تفصيلية للجلسات.
- [ ] **Vercel Redeploy** — البناء الحي قديم (قبل M24–M29)؛ بعد دفع M29 يلزم **Redeploy** من لوحة Vercel ثم تحقق بصري من: DnD باللمس والفأرة، الدفع التلقائي، الإحصائيات الجديدة.- [ ] **ربط Vercel** — متحقّق: المشروع مستورد من GitHub، المتغيرات الأربعة (TURSO_URL/TURSO_TOKEN/ADMIN_PASSWORD/SESSION_SECRET) + BLOB_READ_WRITE_TOKEN مضبوطة، auto-deploy يعمل. (بقيت: تحقق نهائي من تعديل الادمن مع رفع صورة على الرابط الحي.)
- [ ] صور المنتجات المبدئية من `images.unsplash.com` (مخاطرة Hotlinking) — الادمن يستبدلها برابطه؛ **رفع ملفات خارج النطاق** (معيار متفق عليه).
- [x] **CRUD للأصناف نفسها** — تحقق كاملاً في **M16**: إضافة/تعديل/إخفاء/إظهار/حذف/ترتيب Drag & Drop. (كان خارج النطاق سابقاً لأن الأصناف مزرعة وثابتة؛ أصبح إدارة كاملة لأن صاحب المطعم يحتاجها.)
- [ ] حالة/عرض وتأكيد الطلب من الزبون أو حالة (جديد/مكتمل) في الادمن — **خارج النطاق** حالياً (الطلبات مخزّنة تُعرض وتُحذف فقط).

### سجل القرارات (Decisions Log)
- **DB = `node:sqlite`** بدل Prisma 7 (better-sqlite3 بلا prebuild على Node24/win32؛ صفر تبعيات أصلية).
- **صفحات المنيو `force-dynamic`** لضمان انعكاس فوري لتعديلات الادمن.
- **رابط `الإدارة`** في الترويسة للوصول إلى `/admin` (زر 🔒).
- **فحص HTTP للـ auth** عبر `curl` (التحقق بـ PowerShell ضلّل باتباع إعادة التوجيه).
- **مكوّنات الطبق**: الأساسية سعرها 0 (مشمولة) والإزالة لا تُغيّر السعر؛ الإضافات لها سعر يُضاف. يُعاد حساب السعر **خادمياً** عند الطلب (لا يُؤتمن سعر العميل) مع التحقق من انتماء الإضافات للمنتج.
- **الطلبات بلا واتساب**: `NEXT_PUBLIC_WHATSAPP` و`buildOrderText` وزر «واتساب» أُزيلوا؛ الطلبات تُخزّن في `orders` وتظهر في `/admin/orders`.
- **هوية سطر السلة**: `CartLine.key` فريد (منتج + ترتيب الإضافات/المحذوفات) كي لا تندمج التخصيصات المختلفة؛ مفتاح localStorage ارتُقي إلى `menu_cart_v2`.
- **صور الأصناف**: `Category.imageUrl` عبر ترحيل `ALTER TABLE ADD COLUMN` عند الإقلاع للقواعد القائمة (idempotent).
- **العملة**: دج افتراضياً وقابلة للتغيير عبر `CURRENCY` في `.env`.
- **رفع الصور محلياً**: `saveImageUpload` يتحقق من الصيغة (jpeg/png/webp/gif) والحجم (≤4MB) ويحفظ باسم فريد في `public/uploads`؛ `bodySizeLimit` في next.config ليتسع لنقل ملفات النماذج.
- **المكون الإلزامي**: `isRequired` يقفل إزالة المكون في نافذة الزبون (disabled + 🔒) ويستبعد الإضافة قسرياً من `removedList`؛ لا يُطبّق سعر (الرئيسية مشمولة دائماً).
- **صفحات الأصناف الادمن**: `/admin/dashboard` مجرد شبكة بطاقات؛ كل صنف له صفحته `/admin/categories/[slug]` كي تنمو أقسام كل صنف دون ازدحام صفحة واحدة.
- **Turso/Vercel**: `node:sqlite` مدمج يعمل محلياً فقط (ملف) ولا يعمل على Vercel؛ لذلك أُضيفت طبقة `@libsql/client` (async) مع `getDb()` يقرر تلقائياً بناءً على `TURSO_URL`. كل الدوال/السكربتات تحوّلت إلى async بواجهة واحدة؛ السكربتات داخل `async main()`. النشر على Vercel يحتاج `TURSO_URL` + `TURSO_TOKEN` (+ `ADMIN_PASSWORD` + `SESSION_SECRET`) في متغيرات البيئة.
- **الملفات على Vercel للقراءة فقط**: لا تُنشئ مجلدات/ملفات عند التحميل (logger يكتب للـ console عند الفشل)؛ الصور المرفوعة من الادمن تذهب إلى Vercel Blob (وليس `public/uploads`).
- **الإخفاء ≠ عدم التوفّر**: `isHidden` مستقل تماماً عن `isAvailable`؛ غير المتوفر يبقى ظاهراً معتماً في المنيو، والمخفي يختفي كلياً دون حذف. إظهاره لاحقاً إجراء يدوي صريح. `UPDATE ... WHERE categoryId=? AND isAvailable=0 AND isHidden=0` للدفعة، و`IN (...)` مع `changes` (أُضيف لحقل نتيجة `run`) للعدد الحقيقي المتأثر.
- **ثيم الادمن الفاتح معزول**: `html.admin-light` يعيد تعريف متغيرات CSS فقط (لوحة فاتحة/كروت بيضاء/حدود فاتحة/نصوص رمادية داكنة/ظلال) فينعكس على كل صفحة `/admin` والترويسة/التذييل المشتركة عبر `AdminTheme`؛ الزبون يبقى على السمة الداكنة كما هو.
- **بطاقات الأصناف المربعة**: `aspect-square` للزبون والأدمن مع `object-cover`؛ اسم الصنف ظاهر دائماً في شريط تدرّج أسفل البطاقة (ليس عند التمرير فقط)؛ الهاتف `grid-cols-2` بالضبط لأنه الاستخدام الأكثر (مسح ضوئي سريع).
- **ترتيب الأصناف بالـ Drag & Drop**: `Reorder.Group` (axis="y") من motion مع `useDragControls` كمقبض سحب منفصل (حتى لا يتعارض السحب مع حقول النماذج في الصف)؛ الترتيب يُحفظ فقط بزر «حفظ الترتيب» الذي يظهر عند تغيّر الترتيب (`dirty`) لتفادي كتابة عشوائية على كل سحب.
- **إدارة الأصناف عمودياً**: لوحة التحكم صارت قائمة صفوف (صورة مربعة + اسم قابل للتعديل + إخفاء/إظهار + عدد + حذف) بدل شبكة البطاقات — لأن السحب العمودي لا يعمل مع شبكة متعددة الأعمدة؛ «حذف الصنف» يستخدم FK CASCADE (Category→Product→Ingredient) وينظّف صور الأطباق والصنف ثم `redirect` للوحة.
- **slug للأصناف الجديدة**: `makeCategorySlug` يعرّب أسماء عربية شائعة (المقبلات→starters…) ويقعّد `category-<timestamp>` للبقية (فريد تلقائياً)؛ لا يُترجم بعد إعادة التسمية (الـ slug ثابت).
- **تعديل السعر دون صورة**: `uploadImageField` تعيد `null` إذا كان `raw.size === 0` (ملف فارغ = لا اختيار) — فيبقى `updateProductAction` محتفظاً بـ `existing.imageUrl`؛ نفس الحارس يخدم إضافة الأصناف بصورة اختيارية.
- **صفحة تعديل الطبق أكورديون**: كل طبق = 4 أقسام قابلة للطي بحركة (`AnimatePresence` + ارتفاع تلقائي): «معلومات الطبق» مفتوح افتراضياً و«المكونات»/«الإضافات»/«خيارات أخرى» مغلقة لتقليل طول الصفحة؛ المكونات والإضافات تعرض فقط ما يخص هذا الطبق (`productId`). «خيارات أخرى» تضم حذف الطبق. منع الإرسال المزدوج عبر `disabled={pending}` لكل الأزرار + رسالة «تم الحفظ ✓».
- **أنيميشن الطيران للسلة**: الإضافة الفعلية (`add`) تتم أولاً (حالياً) ثم يُطلق تأثير بصري مستقل — فلا يؤخّر الأنيميشن السلة أبداً. مصدر الطيران صورة الطبق (زر التخصيص يؤكد من زر «أضف إلى السلة» داخل الـ Sheet) والهدف زر السلة؛ المسار يُحسب لحظياً عبر `getBoundingClientRect` (بلا إحداثيات ثابتة) والدالة النقية `flyVector` في `lib/cart.ts` قابلة للاختبار (smoke). طبقة الرحلة (`FlyingImage`) تعيش داخل `CartProvider` (z-70 فوق الـ Sheet) وتُزال عند الوصول → نبضة 🛒 + إشعار يختفي ذاتياً (2.4s). إضافات متتالية سريعة = رحلات مستقلة بمعرّفات (بلا تعارض/خطأ).
- **Bottom Sheet لتخصيص الطبق**: هاتف `fixed inset-0 items-end` (انزلاق من أسفل + مقبض سحب) وكمبيوتر `sm:items-center` (Modal موسّط بنفس حركة الانزلاق) — حركة موحّدة عبر spring؛ الجسد قابل للتمرير والتذييل (الإجمالي + التأكيد) ثابت. `AnimatePresence` يغيّر الخروج. لا يتغيّر منطق التخصيص/السعر/الـ key.
- **وضوح وسرعة أنيميشن الطيران (M18)**: قيم الطيران أصبحت ثوابت مجرّدة (`FLY_TARGET_OPACITY`/`FLY_TARGET_SCALE`/`FLY_TRANSITION`) في `lib/cart.ts` قابلة للاختبار في smoke — لأن «واضحاً غير شفاف» و«أبطأ» أهداف قابلة للتحقق آلياً (opacity=1 وduration≥1). انتقل من spring (سرعة عالية) إلى tween بمدة 1.3s و`easeInOut`: تبدأ الحركة وتهدأ ناعمة فيحسّ الزبون بمسار الطبق إلى زر السلة. **Logging لهذا التعديل غير ممكن**: الإعداد في مكوّن Client-side (`"use client"`) بلا مسار لـ `lib/logger.ts` (كتابة ملف خادم)؛ سُجّل القرار هنا بدل الإنذار الكاذب، ولا يؤثر على أي مسار خادم.
- **مسار الطيران مرحلتان (M19)**: استُبدل الخط المستقيم بـ **صعود بطيء ناعم** (easeInOutCubic، 0.85s) ثم **انطلاق سريع بمنحنى**. القوس يتحقق بتكليف إيزنغ **مختلف** لكل محور في مرحلة الانطلاق (أفقي easeOut يسرع أولاً ويتباطأ، رأسي easeIn يتباطأ أولاً ويسرّع) — عبر `transition` لكل خاصية داخل `motion.div` واحد؛ ولهذا أصبح الاختبار يتحقق أن `x.ease ≠ y.ease` (ضمان القوس) و`launch < rise` (ضمان التباين). `FLY_TRANSITION` من M18 أُزيل (صار يتيماً)؛ `FLY_RISE`/`FLY_RISE_TRANSITION`/`FLY_LAUNCH_TRANSITION` بديله. القيم مجرّدة وقابلة للضبط من `lib/cart.ts` دون لمس المكوّن.
- **تصغير بدل الصعود + حواف سوداء (M20)**: استُبدل صعود M19 بالتصغير إلى 50% في مكان الصورة (المستخدم طلب «أن يصغر بنسبة 50/100 بدل أن يصعد»). `FLY_RISE`/`FLY_RISE_TRANSITION` صارا **يتيمين** فأُزيلا؛ البديلان `FLY_PREP_SCALE = 0.5` (المرحلة الأولى تصير `scale: 1 → 0.5` بلا حركة `y`) و`FLY_PREP_TRANSITION` (نفس 0.85s/easeInOutCubic للصعود). مرحلة الانطلاق تبدأ من (0,0) بمقياس 0.5 وتنتهي عند السلة بمقياس `FLY_TARGET_SCALE` — القوس ما زال مضموناً باختلاف إيزنغ المحورين. **الحواف السوداء** تُطبَّق في `FlyingImage` على مستوى العرض (className) لذا لا يمكن اختبارها في smoke (لا DOM) — أُثبتت بالفحص البصري/البناء؛ اختبارات الـ config قابلة للتحقق آلياً (`FLY_PREP_SCALE === 0.5` و`launch < prep` و`x.ease ≠ y.ease`).
- **نظام إدارة الطلبات (M21+)**: بنية المراحل `new → preparing → delivered → completed` + `cancelled` منفصلة. «تأكيد الطلب» يبدأ التحضير فعلياً في سجّل `confirmedAt` و`preparingAt` بنفس اللحظة (المواصفة تريد نقطتي «تم التأكيد» و«قيد التحضير» في الـ Timeline). كل انتقال يكتب حدثاً في `order_activity` موقّعاً بـ actor («system» للإنشاء، «admin» افتراضياً). تأخير المرحلة يُحسب نقيّاً (`minutesInStage` = الآن − بداية المرحلة؛ المراحل الطرفية `null`) والعتبات قابلة للتعديل من `settings` (`late_*_minutes`) — بلا أرقام ثابتة في الكود. بيانات الزبون مؤجلة (قرار المستخدم) لكن أعمدة `customerName/Phone/Address/notes` موجودة فارغة في المخطط لعدم الحاجة لترحيل لاحق. سلسلة حذف الطلب تحذف `order_activity` و`order_line` صراحةً (لا اعتماد على FK في Turso).
- **لوحة الطلبات (M23)**: إجراءات خط الأنابيب في `app/admin/actions.ts` إجراءات Server Action مباشرة (لا Forms) تدعى من العميل مع `router.refresh()` عبر مساعد `run()` — أسرع للعمل اليومي من `useActionState` لكل زر؛ الأمان محفوظ (`isAdmin()` في كل إجراء). الحالة «المكتملة» بلا أزرار سريعة بعد (إعادة/طباعة في M24). الصفحة جاهزة للبحث بالاسم/الهاتف مستقبلاً (حقل البحث يدعم نص البطاقات والـ id حالياً؛ بيانات الزبون قادمة). «تحديث» يدوي + تلقائي كل 30 ثانية حتى M25 (الوصول الفوري مع صوت/شارة).
- **لوحة التفاصيل (M24)**: التفاصيل تُجلب عند الطلب (`getOrderDetailAction` → `OrderDetail`) لا في كل تحديث للوحة — يبقي حمل القائمة خفيفاً. تسميات النشاط (`ACTIVITY_LABELS`) والمسار الزمني مستمد من `order_activity` والطوابع الزمنية نفسها (لا تخزين مكرر). الطباعة كصفحة مستقلة `/print/[id]` خارج `app/admin` لتجاوز التخطيط الإداري (الـ guard يدوياً)، والـ `@media print` يستخدم تقنية `visibility` المخفية للجميع عدا `.print-area` فتحفظ الترويسة والتذييل في الشاشة بلا تعديلهما. لوحة الحقن من `end` (يسار في RTL) كأسلوب شائع بالتطبيقات العربية.
- **الإشعار الفوري (M25)**: SSE استُخدم بدل WebSocket/`@tanstack/query` — لا تبعيات جديدة والاستقصاء كل 4 ثوانٍ كافٍ لمقاومة الطلبات على قاعدة SQLite/Turso. **درس صريح**: Next يحتجز ترويسات `text/event-stream` حتى أول بايت من الجسم، فإذا لم يُرسل الحدث الأول إلا بعد إغلاق الاتصال يعلق الطلب 8+ ثوانٍ — الحل حدث `init` فوري يرسل `lastId` الحالي في أول استقصاء (اختبار معزول بـ `/api/ping` أكد التدفق قبل تبنّي النمط). الحارس `401` (لا `307` redirect) في مسار الحدث كي لا يبتلع EventSource توجيه تسجيل الدخول في إعادة اتصال لا نهائية. `request.signal` هو مصدر الحقيقة للإغلاق (الـ `controller.close()` من `abort`). الصوت عبر Web Audio (مولد نغمات) لا ملف — يعمل حتى بلا أصول.
- **صفحة الإعدادات (M26)**: `saveSettingsAction` نموذج useActionState كباقي نماذج الادمن (الأنسب هنا لأن الحقول محدودة وثابتة) مع `router.refresh()` عند `ok`. القيم تُعرض بـ `defaultValue` من الخادم (لا تحميل Client-side إضافي) والتحقق من النطاق خادمياً رغم وجود `min/max` في الواجهة. مفاتيح `SETTINGS_MINUTES_KEYS` مفردة تُستخدم للمصادقة والكتابة معاً (مصدر حقيقة واحد) — أي عتبة جديدة تُضاف في قائمة واحدة فقط.
- **لوحة الإحصائيات (M27)**: رسوم **SVG يدوية** بلا مكتبات (قرار المستخدم) — منحنى مساحة + Donut + أشرطة داخل `viewBox` ثابت 320×160 فيتوسّع المسار عبر `preserveAspectRatio` افتراضياً. `computeStats` في `lib/stats.ts` نقي ومستقل عن `db` (يأخذ مصفوفات) فاختباره في smoke سهل وثابت. الإيراد يُحسب خصماً من الملغى (`totalCents` صفر) مع بقاء «إجمالي الطلبات» شاملاً الكل. **درس eslint**: `react-hooks/immutability` يمنع `let acc +=` حتى داخل `map` أثناء العرض — الحل حساب الإزاحة التراكمية بـ `slice().reduce()` لكل شريحة (بلا طفرات). اليوم يُشتق محلياً (`startOfDay`) لكل طلب فيمتد الصباح إلى اليوم دون مشاكل النطاق الزمني.
- **تصدير CSV (M28)**: Route Handler للتنزيل (لا Server Action) لأن المستخدم يحتاج `Content-Disposition: attachment` فيستحق أن يكون مساراً يُفتح مباشرة؛ الحارس `401` هنا أيضاً (لا redirect) فيبقى السلوك صريحاً لرابط مباشر. **Excel عربي** يحتاج BOM: `\uFEFF` أول بايت في الاستجابة (StreamReader يستهلكه تلقائياً فيظهر اسم العمود الأول سليماً). القيم الرقمية تُصدَّر «بسيطة» (بلا علامة العملة) حتى تتعامل معها Excel حسابات — التنسيق للعرض يبقى في الواجهة.
- **فصل المكونات عن الإضافات بصرياً**: «المكونات» تعرض كصفوف «مشمول ✓ / محذوف ✗» مع نقطة حالة خضراء/حمراء وشارة «مشمولة في السعر» حتى لا تبدو خيارات إضافية؛ «الإضافات» فقط تبدو خيارات (مربع اختيار مخصص + تظليل ذهبي عند الاختيار + السعر +). الالتباس السابق (مكونات أساسية تظهر كخيارات) زال دون كسر ميزة الإزالة.
- **DnD عبر الأعمدة (M29)**: `@dnd-kit` اختير بدل `motion/Reorder` لأن الأخير يعمل ضمن قائمة واحدة (`axis="y"`) ولا يعبر الأعمدة ولا يدعم `DragOverlay`؛ `PointerSensor` بمسافة تفعيل 6px يغطي اللمس والفأرة (لا حاجة لـ `TouchSensor` منفصل). حفظ الحالة عبر `setOrderStatusAction` عند تغيّر العمود فقط — السحب داخل نفس العمود مجرد إعادة ترتيب محلية بلا كتابة، والإسقاط خارج أي منطقة يُعيد البناء من الخادم (مصدر الحقيقة).
- **الدفع التلقائي عند الإكمال (M29)**: «مكتمل» يعني مُسلَّماً للزبون فمدفوعاً — فيضبط `updateOrderStatus` كل زوج (الحالة → `paid`/`paidAt`) بمركزية واحدة؛ الإرجاع من المكتمل يتراجع تلقائياً (`unpaid` + `paidAt=null`) كقرار معلن لتجنّب طلب «مدفوع» مازال في الطريق. `paidAt` عمود منفصل حتى لا يلتبس وقت الدفع اليدوي بالزمن الزمني للإكمال.
- **إحصائيات تفاعلية (M29)**: Tooltips تحتاج حالة (hover) فتحوّلت `stats-charts.tsx` إلى Client Component كامل — البيانات `serializable` (مصفوفات) فتنتقل كـ props من صفحة الخادم بلا كسر؛ `RevenueChart` انتقل من صفحة الـ stats إلى ملف الرسوم لتوحيد الصيانة. صور الأطباق الشائعة تُربط عبر `productId` (`order_line.productId` → `products.id`) لا بالاسم — اسم الطبق قد يتغير تاريخياً بينما الـ id ثابت.
- **OGTX Desktop — أمان محلي بالكامل**: كلمات المرور لا تُخزَّن نصاً في أي طبقة (scrypt بـ salt فريد في العملية الرئيسية)؛ الجلسة كائن في الذاكرة بلا ملفات/cookies؛ واجهة الإدارة **لا تُؤتمن** — كل عملية تُحرس بالصلاحيات في main process (`roleCanAccess` + تحقق المالك لكل تعديل على المستخدمين)، فالـ renderer المُخترَق لا يتجاوزها.
- **OGTX Desktop — نفس قاعدة بيانات الويب**: لا قاعدة جديدة — `lib/*` يُعاد استخدامها كما هي فيعمل Desktop وموقع الزبون على **Turso نفسها** (`TURSO_URL` من `.env` الجذر) فتظهر طلبات الموقع في اللوحة فوراً. الإصدار المحلي (`sqlite`) مسار تطوير فقط.
- **OGTX Desktop — بيئة/أوضاع**: `env.ts` تحمّل `desktop/.env` ثم `.env` الجذر (لا أسرار في الكود)؛ `OGT_DATA_DIR` يوجّه البيانات والسجلات (القراءة أصبحت كسولة بعد أن كانت عند تحميل الوحدة)؛ وضع `--smoke` يعزل نفسه بقاعدة مؤقتة تُمحى كل تشغيل فلا يلوّث Turso الحقيقي.
- **OGTX Desktop — قرارات بناء**: `electron-vite` (يدمج main/preload/renderer ويبقي `dependencies` خارجية) + `electron-builder` → مثبّت NSIS بالعربية (1025) مع اختيار مجلد التثبيت؛ `@libsql/client` تبقى dependency تُضمَّن في الحزمة. **درس**: npm على هذه الآلة يحجب سكربتات postinstall — binaries الإلكترون/electron-builder تُثبَّت يدوياً (`node node_modules/electron/install.js`) والاختبارات/البناء تعمل بها.
- **OGTX Desktop — إصلاح typecheck للويب والـ renderer معاً**: `lib/orders.ts` كان يستورد نوعاً من `./db` فيجذب `node:sqlite`/`node:path` إلى فحص `tsconfig.web` للـ renderer فيفشل — أصبح مستقلاً بنيوته المحلية (لا يبدّل الواجهة)، و`lib/users.ts` تعيد تصدير الأدوار من `lib/roles.ts` النقي كي لا يُحمَّل `lib/db` في الواجهة.
- **ظلال مخصصة (M29)**: `shadow-soft`/`shadow-card` عُرّفتا كـ `--shadow-*` داخل `@theme` (فيكونان كلسي Tailwind حقيقيين ولا يكلسان البناء) بقيم أفتح تلقائياً في `html.admin-light` عبر إعادة تعريف المتغير في ذلك النطاق — الظلال الداكنة الثقيلة كانت ستلوّث الواجهة الفاتحة.
- **درس client/server (M29 إصلاح عطل)**: بعد تحويل `stats-charts.tsx` إلى `"use client"` كسر صفحة الإحصائيات بـ 500: صفحة الخادم كانت تستدعي `formatCents()` (دالة عادية) مُصدَّرة من موديول client — Next يمنع استدعاء دالة client من الخادم («Attempted to call formatCents() from the server…»). الحل: استيراد `formatPrice` من `lib/utils` (مشترك) في صفحة الخادم مباشرة وحذف `formatCents` المكرر. درس ثانٍ: **تجميع `next build` لا يلتقط أخطاء `points[-1].x`** في أوساط الاستهداف (rect) — خطأ وقت تشغيل بحت؛ أُصلح بتقسيم حدود كل عمود (`midBefore/midAfter`) بدل مؤشرات `i±1` غير المحروسة. والتحقق الفعلي كان عبر `next start` + curl بجلسة موقعة (توليد التوكن عبر `tsx` مع `dotenv/config` لأن `tsx` لا يحمّل `.env` بنفسه).
- **OGTX Desktop — Kanban (MD7)**: (1) **نفس بنية DnD للويب** أُعيد استخدامها (SortableContext داخل كل عمود + DragOverlay + `suppressClick` بعد الإفلات) لكن على حالة مسطّحة واحدة (`orders`) — الترحيل بين الأعمدة تفاؤلي عبر `patchOrder` ثم يُثبّت `updateOrderStatus`، والترتيب داخل العمود مرتب حسب الخادم (المتأخر أولاً ثم الأقدم) بلا كتابة ترتيب محلي (لا عمود ترتيب في DB). (2) **«المعالج»** عبر `listOrderSummaries()` الجديدة في `lib/db` (ضمّ فرعي لأحدث `order_activity`) — إضافة فقط بلا تغيير `listOrders` حتى لا تتأثر لوحة الويب/الإحصائيات. (3) **الدفع/الأولوية/الإلغاء/الرجوع = مدير أو مالك** (`canManageOrders`) مع منع «سحب» البطاقات الملغاة؛ ترتيب فحص في المعالج: صحة الحالة ← قيود الإلغاء ← فحص الرجوع ← التنفيذ. (4) **استقصاء 6 ثوانٍ** (الموثّق كأفضل طريقة متوافقة مع `node:sqlite`/Turso؛ لا SSE من renderer لأن كعكة ادمن الويب ليست متاحة داخل Electron) مع كشف «الطلب الجديد» بـ `id > lastMaxId` (أول تحميل يملأ المرجع دون تنبيه) وToast + صوت Web Audio مُخنّق وBadge حي. (5) **`shell.openExternal` مقيّد** بقائمة بروتوكولات (http/https/tel/mailto) عبر `new URL` حتى لا يستغل renderer مخترق رابطاً عشوائياً.
- **OGTX Desktop — صندوق النقود (MD8)**: كل مبلغ cents صحيح واحد فقط (لا أرقام كسرية) والإشارة تعبير عنها `direction` منفصل حتى تبقى العمليات «صافي +1000» صحيحة؛ عمليات المعاكسة تُخزَّن **لا تُحذف** الأصول (قابلية تدقيق) وتُستبعد من الرصيد/الملخص عبر شرط `correctsTxId IS NULL` فتتطابق الملخصات مع الرصيد الفعلي (لا انفصال بين «إيرادات» و«الدرج»). التصحيح يتحقق «سبب مطلوب» والاسترجاع التلقائي للطلبات يُقيّد «الطلب ليس مدفوعاً» — فالأصل الملغي يحتفظ بسجله والمرآة تبقى الشاهد. الفترة في الملخص تقتصر على ≤32 يوماً للرسم اليومي (تجنيب موجة بيانات عند فترات طويلة). فتح/إغلاق الجلسة صار عنصر تحكم **واحد** («فتح صندوق جديد»/«إغلاق الصندوق») حسب حالة الجلسة الحالية — لا إجراءان متنافران على الشاشة.
